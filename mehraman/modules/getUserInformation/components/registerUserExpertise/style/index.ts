import styled from 'styled-components'

export const RegistrationUserExpertiseContainer = styled.div`
  min-height: calc(100vh - 128px);

  .row {
    height: calc(100vh - 128px);
  }
  .formContainer {
    background-color: white;
    padding: 32px;
    opacity: 0.9;
    box-shadow: 0px 15px 60px rgba(0, 0, 0, 0.05);
    border-radius: 8px;
    margin-top: 0;
  }
  .form-item {
    margin-top: 15px;
  }
`
